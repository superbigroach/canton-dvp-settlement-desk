# VITE_AUTH_MODE=firebase|sandbox   (default firebase; sandbox sends X-Sandbox-User, dev only)
# VITE_API_MOCK=1                   (in-memory mock of docs/PRODUCT-PLAN.md §5)
# VITE_FIREBASE_API_KEY / VITE_FIREBASE_AUTH_DOMAIN / VITE_FIREBASE_PROJECT_ID / VITE_FIREBASE_APP_ID
#   override the crossdesk-devnet-app web config baked into src/auth/firebase.ts
