apiVersion: v1
kind: Service
metadata:
  name: {{ include "canton-dvp-desk.fullname" . }}
  labels:
    {{- include "canton-dvp-desk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "canton-dvp-desk.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
